<%  include partials/header %>
<div class="ui main text container big inverted form segment">
    <div class="ui huge header">ADD A BLOG <i class="edit outline icon"></i></div>
        <form class="ui big form"action="/blogs" method="post">
            <div class="field">
                <label>Title</label>
                <input type="text" name="blog[title]" placeholder="Blog Title"></input>
            </div>
            <div class="field">
                <label class>Image</label>
                <input type="text" name="blog[image]" placeholder="Image for Blog"></input>
            </div>
            <div class="field">
                <label>Body</label>
                <textarea name="blog[body]" placeholder="Content of Blog"></textarea>
            </div>
            
            <button class="ui inverted big button purple" type="submit">ADD BLOG</button>
        </form>
    </div>
</div>

<%  include partials/footer %>